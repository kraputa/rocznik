.scroll-panel-header
  = link_to "Wydania", issues_path, class: %w{btn btn-default btn-sm pull-right}
  = link_to "Dodaj wydanie", new_issue_path, class: %w{btn btn-default btn-sm pull-right}
  - if @issue.published?
  - else 
    = link_to "Edytuj", edit_issue_path, class: %w{btn btn-default btn-sm pull-right}
  %h3.scroll-panel-title
    = "#{@issue.year}/nr.#{@issue.volume}"
  
.scroll-panel-body
- if @issue.published?
- else 
    %h3 Zgłoszone artykuły
    %table.table.table-hover.table-admin
      %thead
        %tr
          %th Nazwisko
          %th Tytuł
          %th
      %tbody
        - @issue.submissions.each do |submission|
          %tr
            %td= submission.title
       
  
